{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch (lldb)",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Build (clang++ -std=c++17)"
    },
    {
      "name": "Launch (cppdbg - lldb)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Build (clang++ -std=c++17)",
      "miDebuggerPath": "/usr/bin/lldb",
      "MIMode": "lldb"
    }
  ]
}
